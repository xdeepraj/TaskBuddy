import { Typography } from "@mui/material";

const BoardView = () => {
  return (
    <div>
      <Typography variant="body1">📌 Board View Content</Typography>
    </div>
  );
};

export default BoardView;
